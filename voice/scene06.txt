Every task. Every reward. Filter by hard — 97 toughest. Search saboteur — adversarial duels where a bot re-injects faults so a single fix isn't enough. Click any card — description, design intent, ground-truth actions, per-shard reward curve. This one went from minus point 6 to plus point 3. It learned. Category chart at the bottom — Lambda throttling, solid. Cascading failures, still hard.
