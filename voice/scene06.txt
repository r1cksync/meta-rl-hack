Every task. Every reward. Filter by hard. Search saboteur — adversarial duels where a bot keeps re-injecting faults, so a single fix is never enough. Click any card — full description, design intention, ground-truth action chain, and the per-shard reward trajectory. The hard ones really are hard.
